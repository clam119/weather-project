{
  "name": "weather-project",
  "version": "1.0.0",
  "description": "A basic weather application that uses the OpenWeatherMap API to pull data relating to user location.",
  "main": "weatherApp.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christopher Lam",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  }
}
